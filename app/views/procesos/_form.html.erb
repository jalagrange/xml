<% form_for(@proceso) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
   	<%= f.label :software_id %>
	<%=f.collection_select(:software_id,Software.all,:id, :name) %>
  </p>
  <p>
    <%= f.submit button_label %>
  </p>
<% end %>